apiVersion: v1
kind: Service
metadata:
  name: frontier-bot
  namespace: default
spec:
  ports:
  - name: http
    port: 8999
    targetPort: 8999
    protocol: TCP
  selector:
    app: frontier-bot
  type: LoadBalancer